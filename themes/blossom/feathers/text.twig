{% extends "content/post.twig" %}

{% block content %}
      {% if post['title'] %}
      <h2>
        <a href="$post.url" rel="bookmark" title="${ "Permanent link to" | translate } &quot;$post.title&quot;">$post.title</a>
      </h2>
      {% endif %}
      <section{% if post.body | length > 1000 %} class="columnized"{% endif %}>
        ${ post.body | read_more("…more") }
      </section>
{% endblock %}
