{% extends "content/post.twig" %}

{% block content %}
      {% if post['title'] %}
      <h2 class="post_title">
        <a href="$post.url" rel="bookmark" title="${ "Permanent link to" | translate } &quot;$post.title&quot;">$post.title</a>
      </h2>
      {% endif %}
      <section class="description{% if post.description | length > 1000 %} columnized{% endif %}">
        ${ post.description | read_more("…more") }
      </section>
      <section class="video">
      	$post.video_player
      </section>
{% endblock %}
